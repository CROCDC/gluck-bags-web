[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra
markers =
    perf: browser-based performance / Core-Web-Vitals tests (slow; spin up a real server).
    lighthouse: official Lighthouse CLI audit (slow; needs npx/Chrome).
    e2e: end-to-end browser flows driven through Playwright.
